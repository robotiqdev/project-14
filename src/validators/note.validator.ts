import { z } from 'zod';

// Minimal stub — full implementation to be provided by the implementation developer.
// This file exists only so that existing tests compile.
export const UpdateNoteSchema = z.object({}).strict();
