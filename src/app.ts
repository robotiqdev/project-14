import * as express from 'express';

// Minimal stub — full implementation to be provided by the implementation developer.
// createApp() should wire: express.json(), note router at /notes, error handler.
export function createApp(): express.Application {
  const app = express();
  return app;
}
