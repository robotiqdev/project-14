import { Router } from 'express';

const router = Router();

// Stub routes — not implemented
// GET / → listArchivedTasks
// GET /:taskId → getArchivedTask
// POST /:taskId/restore → restoreTask

export default router;
